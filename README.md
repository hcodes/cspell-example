# Пример подключения CSpell к проекту

## Установка
`npm i --save-dev cspell`  
`npm i --save-dev @cspell/dict-ru_ru`

Подробности в [конфигах](./.config).


## Использование
`npm run cspell-lint`

## Сортировка слов в собственном словаре
`npm run cspell-sort`
